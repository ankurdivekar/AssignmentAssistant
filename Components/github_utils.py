

def get_repo_url(user_name, repo_name):
    return f'https://github.com/{user_name}/{repo_name}'


def get_file_url(user_name, repo_name, file_name):

    return f'https://raw.githubusercontent.com/{user_name}/{repo_name}/master/{file_name}'
